/**
En el siguiente práctico crearemos un módulo que contenga una función 
que pueda ser exportada, mediante  ESmodules
*/

//Creamos una función
export function multiplicar(a,b) {  
    return a*b;
}
export function dividir(a,b) {  
    return a/b;
}
